base_url = "https://acm.umn.edu/"
build_search_index = false
compile_sass = true
generate_rss = true
highlight_code = true
title = "ACM UMN"

# Enable if you want to check, but this is slow and dip doesn't send us back logs afaik.
# check_external_links = true

taxonomies = [
    {name = "tags", rss = true}
]

[extra]
nav_links = [
	{ url = "/room", text = "Room" },
    { url = "/events", text = "Events" },
    { url = "/contact", text = "Contact" },
    { url = "/newsletters", text = "Newsletters" },
    { url = "/resources", text = "Resources" },
    { url = "/involvement", text = "Get	Involved" },
    { url = "/mascot", text = "Mascot" }
]
